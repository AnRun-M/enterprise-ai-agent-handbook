# ADR-0006：AI 协作依赖仓库记忆，不依赖聊天记忆

## 状态

Accepted

## 背景

AI 工具的聊天上下文不持久，跨会话的决策、术语与架构事实必须在仓库内可恢复。

## 决策

所有 AI 在修改仓库前必须读取 `AGENTS.md` 与 `.ai/context/`；`docs/adr/` 是 ADR 的唯一事实源。

## 影响

- 任何 AI 的首次操作是读取，而不是修改
- 记忆文件的更新按影响范围分级，避免礼仪式更新
- 单一事实源规则保证决策记录不产生分叉
