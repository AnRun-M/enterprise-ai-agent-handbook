# ADR-0003：LangGraph 是核心实践框架，但不是唯一主题

## 状态

Accepted

## 背景

框架 API 的获取成本极低，若全书围绕单一框架展开，会削弱对 Agent Runtime 本质的理解。

## 决策

LangGraph 作为核心实践框架贯穿全书，但章节不绑定单一框架；手写实现与 LangGraph 等价实现并行讲解。

## 影响

- 每个抽象先讲手写方案，再讲 LangGraph 抽象
- 框架能力按需引入，不按 API 罗列
- 评估其他框架时以同一套架构标准衡量
