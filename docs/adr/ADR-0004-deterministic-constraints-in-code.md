# ADR-0004：确定性约束优先由代码保证

## 状态

Accepted

## 背景

模型输出具有概率性，涉及安全、成本与合规的约束若依赖模型自律，存在不可接受的风险。

## 决策

以下约束由程序强制保证，不依赖 Prompt 或模型意志：

- SQL 安全
- 权限校验
- 输出格式
- 超时
- 行数限制
- 引擎路由
- 重试策略

## 影响

- 所有示例的约束层必须是可测试的确定性代码
- Prompt 只负责模型能可靠完成的部分
- 约束与模型解耦，支持依赖注入
