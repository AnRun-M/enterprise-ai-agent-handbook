# Examples

1. `manual_agent_loop`
2. `basic_langgraph`
3. `text2sql_state`
4. `sql_validation`
5. `checkpoint_hitl`

所有示例必须可运行、可测试、默认安全。

示例目录采用可标准导入的 Python 包命名（无数字前缀），示例顺序由本文档与 `docs/00-introduction/content-map.md` 维护。示例与章节、测试的对应关系见 `docs/00-introduction/content-map.md`。
